# ── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
*.pyo
.pytest_cache/

# ── Virtual Environments ─────────────────────────────────────
venv/
.venv/
env/
ENV/
.conda/

# ── Secrets & Config ─────────────────────────────────────────
.env
*.env.local
*.env.production
secrets.json
config_local.py

# ── Model Artefacts (large binaries) ─────────────────────────
saved_models/*.pkl
saved_models/*.bin
saved_models/*.pt
saved_models/*.h5
!saved_models/disease_classifier.pkl
!saved_models/tfidf_vectorizer.pkl
!saved_models/label_encoder.pkl
.hf_cache/

# ── Data (raw CSVs — check policy before committing) ─────────
data/*.csv
data/*.json
# Exception: the small, hand-built demo metadata subset is meant to be
# tracked so the project runs out of the box — see data/README_DEMO_DATA.md
!data/release_evidences.json
!data/release_conditions.json
!data/README_DEMO_DATA.md

# ── Logs ─────────────────────────────────────────────────────
logs/
*.log

# ── IDE / Editor ─────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Jupyter ──────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb
